"""Authored source content for the supplementary pre-boarding corpus documents.

Each module exposes DOC (metadata) and BLOCKS (the document body as a list of
renderer directives). scripts/build_preboarding_corpus.py turns these into the
PDFs + .meta.yaml sidecars under data/raw/.
"""
